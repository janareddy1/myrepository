pipeline{

  properties {
  
     
   git committer : 'janareddy1'  
    
  }
  
  parameters{
  
  choice(choices : ['teams','prod','stage'], NAME : EnvironmentName, Description : 'environments for API')
   choice(choices : ['Eastus2','centralus'], NAME : Location, Description : 'Location for API')
    
  }


  stages{
  
    stage('git clone'){
    
      steps{
       
        ///git url,git credentials
        
      
      }
    
    }
    
  
  
  
  
  
  }

























}
